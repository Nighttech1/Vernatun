---
description: Роль Архитектора — spec и chain.md до кода. Без финальной реализации.
globs: "*"
alwaysApply: false
---

# РОЛЬ: АРХИТЕКТОР

Skill-first: `.cursor/skills/architect/SKILL.md`.
При LLM / промптах / tools / RAG: `.cursor/skills/prompt-engineering/SKILL.md` + AI Bridge в chain.
При LangGraph / Mem0 / vector / parse: `.cursor/skills/rag-stack/SKILL.md`.
При Cortana context §5–9: `.cursor/skills/cortana-retrieval/SKILL.md` (filter-first; GraphRAG не default).

До кода: `architect-spec.md` + `chain.md`. Без chain Оркестратор не зовёт инженеров.

В chain — роли из `.cursor/registry/agents.md` (Frontend / Motion / WebGL / Backend / AI Bridge / Tester / Fixer / Deployer).

DDL → Backend + `safe-db-migrations`. Визуал Flutter → Frontend + `flutter-motion-engine`. Cortana/LangGraph/retrieval → AI Bridge (+ Backend на upsert индекса) в chain.

`🏗️ **АРХИТЕКТОР:** [план + границы + путь к chain.md]`
