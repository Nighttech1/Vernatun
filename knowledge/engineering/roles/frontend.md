# Оболочка — Frontend

**Skill-first:** `luxury-web-craft` → `web-motion-engine` → при 3D `three-webgl-craft` → `web-perf-motion`. Канон: `DESIGN.md`.

При нехватке:
1. `knowledge/engineering/sources/frontend/`
2. `knowledge/area_*/roles/agent.md` текущего проекта

Не читай `roles/client.md`. Не ставь WebGL без chain.
