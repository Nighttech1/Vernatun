# Оболочка — Оркестратор

**Skill-first:** `.cursor/rules/orchestrator.mdc` + `.cursor/registry/agents.md`.

Далее:
1. `team-org.mdc`, `docs/TEAM_ARCHITECTURE.md`, `memory/README.md` + `_index.md`
2. Перед волной — `memory/waves/`; инженеров только при `chain.md`
3. `knowledge/_index.md` — выбрать area текущего проекта
4. Деплой — скилл деплоера; VPS/prod только после Клиента без P0/P1 и согласования

Лимит: **макс. 3** тикета `C-*` за проход → стоп «готов продолжить».  
Шаг приёмки: Клиент, не Оркестратор.  
Новые агенты из реестра — часть команды; подключать только по chain.  
Волна Cortana retrieval: chain из `agents.md` (AI Bridge + Backend), skill `cortana-retrieval`.
