# Оболочка — Backend

**Skill-first:** `.cursor/skills/backend/SKILL.md` + `zero-data-loss.md`. DDL: `safe-db-migrations`.  
MCP sqlite/postgres — только SELECT/PRAGMA.

Не затирай пользовательские данные. Не пиши в БД через MCP.
